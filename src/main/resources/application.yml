spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver #tradutor de objeto java para jdbc
    url: jdbc:mysql://localhost:3306/learn_kotlin_apirest?useTimezone=true&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
